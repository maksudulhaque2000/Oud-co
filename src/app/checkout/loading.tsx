import { PageLoadingSkeleton } from "@/components/LoadingSkeletons";

export default function Loading() {
  return <PageLoadingSkeleton title="Loading checkout" subtitle="Preparing your saved customer details and summary." />;
}
